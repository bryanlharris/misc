# hosts		This file describes a number of hostname-to-address
#		mappings for the TCP/IP subsystem.  It is mostly
#		used at boot time, when no name servers are running.
#		On small systems, this file can be used instead of a
#		"named" name server.  Just add the names, addresses
#		and any aliases to this file...
# By the way, Arnt Gulbrandsen <agulbra@nvg.unit.no> says that 127.0.0.1
# should NEVER be named with the name of the machine.  It causes problems
# for some (stupid) programs, irc and reputedly talk. :^)

# For loopbacking.
127.0.0.1		localhost
10.3.3.65		bryanslack bryanslack.corp.neospire.net bryanslack w.bryanslack d.bryanslack git.bryanslack
# End of hosts.

# for t in $(g --tickers | tr '\n' ' '); do g $t |awk '{printf "%-16s'$t'-%-30s'$t'%-30s\n",$4,$3,$3}'; done \
# | sed 's/volunteersolutions/+v/g' | grep -v -e NULL -e '^ ' -e '^[^0-9]'

# for t in $( g --tickers | tr '\n' ' '); do
#     g $t | awk '{$0 !~ /^( )|(.*NULL)|([^0-9]/ printf "%-16s'$t'-%-30s'$t'%-30s\n",$4,$3,$3}'
# done | sed 's/volunteersolutions/+v/g' | grep -v -e NULL -e '^ ' -e '^[^0-9]'
